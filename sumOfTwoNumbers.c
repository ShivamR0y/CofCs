//program for sum of two numbers

#include <stdio.h>
int main()
{
        float num1,num2;
        printf("Enter two numbers to find their sum: ");
        scanf("%f",&num1);
        scanf("%f",&num2);
        printf("The sum of the two nubmers is: %f",num1+num2);
        
        return 0;
}
